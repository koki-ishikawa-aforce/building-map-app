FROM node:18-alpine

WORKDIR /app

# システムパッケージのインストール
RUN apk add --no-cache \
    git \
    curl \
    wget

# 依存関係をインストール
COPY package.json package-lock.json* ./
RUN npm install

# ソースコードをコピー
COPY . .

# 開発サーバーのポートを公開
EXPOSE 3000

# 開発サーバーを起動
CMD ["npm", "run", "dev"] 